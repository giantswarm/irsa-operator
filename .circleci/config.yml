version: 2.1

orbs:
  architect: giantswarm/architect@4.13.0

workflows:
  build:
    jobs:
      - architect/go-build:
          name: go-build
          binary: irsa-operator
          filters:
            tags:
              only: /^v.*/

      - architect/push-to-docker:
          context: "architect"
          name: push-irsa-operator-to-quay
          image: "quay.io/giantswarm/irsa-operator"
          username_envar: "QUAY_USERNAME"
          password_envar: "QUAY_PASSWORD"
          requires:
            - go-build
          # Needed to trigger job also on git tag.
          filters:
            tags:
              only: /^v.*/
      - architect/push-to-docker:
          context: "architect"
          name: push-irsa-operator-to-aliyun
          image: "registry-intl.cn-shanghai.aliyuncs.com/giantswarm/irsa-operator"
          username_envar: "ALIYUN_USERNAME"
          password_envar: "ALIYUN_PASSWORD"
          requires:
            - go-build
          # Needed to trigger job also on git tag.
          filters:
            tags:
              only: /^v.*/

      - architect/push-to-app-catalog:
          context: "architect"
          name: push-irsa-operator-to-app-catalog
          app_catalog: "control-plane-catalog"
          app_catalog_test: "control-plane-test-catalog"
          chart: "irsa-operator"
          requires:
            - push-irsa-operator-to-quay
          # Needed to trigger job also on git tag.
          filters:
            tags:
              only: /^v.*/
